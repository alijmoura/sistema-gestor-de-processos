import {
  initializeProcessosPage,
  refreshProcessosPage,
  disposeProcessosPage
} from '../main.js';

export default {
  initialize: initializeProcessosPage,
  refresh: refreshProcessosPage,
  dispose: disposeProcessosPage
};
